/**
 * A map of validator names used in the `validate` options block on sequelize models 
 * to validator.js functions used to perform the respective validation
 */
// TODO don't need this, can just use their map
const sequelizeValidatorJsValidator = require("sequelize/lib/utils/validator-extras").validator;
